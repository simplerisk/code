import { createContext, useContext } from "react";
export const GridStackWidgetContext = createContext(null);
export function useGridStackWidgetContext() {
    const v = useContext(GridStackWidgetContext);
    if (!v) {
        throw new Error("useGridStackItem / useWidgetSerializer must be used inside <GridStackItem> content");
    }
    return v;
}
